{
  "name": "golightly03",
  "private": true,
  "scripts": {
    "build": "npm --prefix db-models run build && npm --prefix api run build && npm --prefix web run build",
    "test": "npm --prefix db-models test && npm --prefix api test",
    "lint": "npm --prefix web run lint",
    "typecheck": "npm --prefix db-models run typecheck && npm --prefix api run typecheck && npm --prefix web run typecheck",
    "clean": "npm --prefix db-models run clean"
  }
}
